name: Automated

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Azure login
      uses: Azure/login@v2.0.0
      with: 
        ${{ secrets.AZURE_CREDENTIALS }}
